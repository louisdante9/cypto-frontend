import React from "react";

const ToggleProvider = React.createContext(false);

// You can also import and use it like that
// export const { Provider, Consumer } = ToastProvider;

export default ToggleProvider;